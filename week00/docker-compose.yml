services:
  week00:
    build: .
    environment:
      - TZ=Asia/Seoul
    ports: 
      - "8000:8000"
    networks: 
      - week00
    volumes:
      - .:/app
    command: bash -c "uvicorn app.main:app --host 0.0.0.0 --port 8000"
networks:
  week00:
    driver: bridge